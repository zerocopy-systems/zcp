[package]
name = "zero-copy-utils"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
bytes = "1.5"
libc = "0.2"
thiserror = "1.0"
tokio = { version = "1.35", features = ["full"] }
nix = { version = "0.27", features = ["fs", "ioctl"] }
